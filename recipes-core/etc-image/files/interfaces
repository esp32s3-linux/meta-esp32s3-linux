# ESP-Hosted exposes the station interface as espsta0.
auto lo
iface lo inet loopback

auto espsta0
iface espsta0 inet dhcp
	pre-up wpa_supplicant -B -i espsta0 -c /etc/wpa_supplicant.conf
	pre-down killall -q wpa_supplicant
